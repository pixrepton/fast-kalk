# Copy to local-secrets.env (gitignored). configure-local.php can populate from cieplo-orchestrator/.env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_SSL=false
SMTP_USE_TLS=true
